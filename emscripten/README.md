Code to allow building SageMath to WASM using Emscripten. (Under Construction)

Files:

- `build.sh`: Run as `emscripten/build.sh` in root directory of SageMath. Requires installation of 'autoconf-archive' 'mercurial'. Recommended emsdk version: 3.1.23.